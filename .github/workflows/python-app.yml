# .github/workflows/python-app.yml (минимальная версия)
name: Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run minimal test
        run: python -c "print('All good')"
